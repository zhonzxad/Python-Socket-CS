Notes:
0. 服务端程序在server文件夹下的server.exe，客户端程序在client文件夹下的client.exe
（注意程序运行目录不要出现英文，并且允许程序获取网络权限）
1. 首先在任意一台电脑上启动服务端程序，记下此电脑的IP地址
2. 打开客户端程序，输入服务端的IP地址
（注意保证打开客户端程序的电脑和打开服务端程序的电脑是相通的）
3. 之后客户端按照固定的格式就可以通过服务器程序中转来进行消息发送
（IP|Msg， 期望接收消息的IP地址 | 需要发送的消息）
4. 当客户端输入exit就可以退出发送